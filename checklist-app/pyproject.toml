[project]
name = "learning-path-checklist-app"
version = "0.1.0"
description = "Reusable learning path checklist app with Flask"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "flask>=3.1.0",
  "markdown>=3.8"
]

[tool.uv]
package = false
